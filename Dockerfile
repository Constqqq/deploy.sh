# 1. Используем легкий официальный образ Python
FROM python:3.11-slim

# 2. Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# 3. Копируем файл зависимостей и устанавливаем их
# Делаем это отдельным шагом для кэширования слоев
#COPY requirements.txt .
#RUN pip install --no-cache-dir -r requirements.txt

# 4. Копируем остальные файлы проекта (ваш скрипт)
COPY . .

# 5. Команда, которая запустится при старте контейнера
CMD ["python", "app.py"]
